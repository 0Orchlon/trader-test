{
  "name": "personal-3-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "PERSONAL-3 Alpaca trading system — нийтлэгдсэн гэрээ (OpenAPI 3.1 + AsyncAPI 3.0 + Tool Contract v1) ба prism дуурайлт",
  "type": "module",
  "scripts": {
    "lint": "redocly lint openapi.yaml",
    "bundle": "node bundle.mjs",
    "check:bundle": "node bundle.mjs --check",
    "mock": "prism mock openapi.yaml -p 4010",
    "mock:dynamic": "prism mock openapi.yaml -p 4010 -d",
    "docs": "redocly build-docs openapi.yaml -o site/index.html",
    "verify:mock": "node verify-mock.mjs",
    "test": "npm run lint && npm run check:bundle && npm run verify:mock"
  },
  "devDependencies": {
    "@redocly/cli": "2.53.2",
    "@stoplight/prism-cli": "5.16.0"
  }
}
